//dependencies
import {createStaticNavigation} from '@react-navigation/native';

import SplashNavigator from './splash.navigator';

const SplashNavigation = createStaticNavigation(SplashNavigator);

export default SplashNavigation;
